import 'package:flutter/material.dart';

class FlightsCards extends StatelessWidget {
  const FlightsCards({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
